VITE_API_BASE_URL=http://localhost:5063/api

# Fill in the project DSN to enable Sentry error reporting. Empty (or absent) means Sentry never
# initializes and nothing leaves the browser -- see src/main.tsx.
VITE_SENTRY_DSN=
